<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Speech-to-text engine: used by keyboard mic, other STT callers,
             and declared as the recognitionService for the VoiceInteractionService. -->
        <service
            android:name=".FreeSpeechService"
            android:exported="true"
            android:permission="android.permission.BIND_RECOGNITION_SERVICE">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <!-- Voice assistant entry point: makes FreeSpeech the system digital
             assistant. Android Auto's microphone button calls this when no
             Google Assistant is present. -->
        <service
            android:name=".FreeSpeechVoiceInteractionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/interaction_service" />
        </service>

        <!-- Creates VoiceInteractionSession instances for each invocation. -->
        <service
            android:name=".FreeSpeechVoiceInteractionSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION_SERVICE" />

        <!-- Android Auto car app — appears in the AA launcher on the car screen.
             Category POI is the broadest available; there is no "voice assistant"
             category in the Car App Library. The HostValidator in the service is
             set to ALLOW_ALL_HOSTS so sideloaded builds work without certificate
             pinning. Unknown-sources mode must be enabled once in AA dev settings. -->
        <service
            android:name=".FreeSpeechCarAppService"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
            <meta-data
                android:name="androidx.car.app.minCarApiLevel"
                android:value="1" />
        </service>

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
